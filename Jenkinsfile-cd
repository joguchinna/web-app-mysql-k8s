pipeline {
    agent any
     parameters {
        // String Parameter
        string(name: 'IMAGE_TAG', defaultValue: '', description: 'Image tag')
   }
    stages {
        stage('Verify deployment details') {
            steps {
                script {
                    // You can add deployment verification logic here
                    println "Verifying deployment details..."
                }
            }
        }

        stage('Web app deployment') {
            steps {
                script {
                        sh """
                          docker images
                          docker run -itd --name app -p 9090:5000 637423446577.dkr.ecr.us-east-1.amazonaws.com/web-app:${params.IMAGE_TAG}
                       """
                    }
                }
            }
    }
}
